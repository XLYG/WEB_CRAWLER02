# 德国大学爬虫 — 从零构建指南

## 一、项目概览

### 1.1 目标任务是什么？

建立一个**结构化的德国大学硕士专业信息数据库**。每条记录的粒度为"某所大学的某一个硕士专业"，包含：

- 专业描述（中/英/德语混合，保留原文）
- 学位变体信息（如 Master consecutive、Bachelor 50% 等）
- 入学申请要求
- 模块手册（Modulhandbuch / Module Handbook，PDF 原文）
- 入学考试规定（Prüfungsordnung / Zugangsordnung，PDF 原文）
- 原始网页 URL（可溯源）

### 1.2 覆盖规模

| 批次 | 日期 | 大学数 | 产出 |
|------|------|--------|------|
| 1-27 | 1月27日 | 5 + RWTH Aachen | 基础爬虫 + PDF |
| 2-2 | 2月2日 | 8 | requests/lxml 脚本 |
| 2-9 | 2月9日 | 8 | Playwright 脚本 + 缺漏补量 |
| 2-24 | 2月24日 | 8 | 多代架构自适应 |
| 3-2 | 3月2日 | 8 | 混合模式 + 注释 |
| 3-16 | 3月16日 | 8 | 哨兵切片模式 |
| 3-23 | 3月23日 | 8 | 批量规范化 |
| 3-30 | 3月30日 | 3 | 包结构 + 纯 MD |

合计覆盖 58+ 所德国大学**，生成 **~1200+ Markdown 文件**和 **~500+ PDF 文件**。

### 1.3 为什么要自己做而不是用现成工具

- 德国大学网站**没有统一 CMS**，每家结构完全不同
- 即使同一大学，不同专业页面也可能属于不同子系统
- 大量内容依赖 JS 动态渲染（React、Vue、自定义框架）
- 需要按统一 schema 重新组织数据，原始页面质量参差不齐

---

## 二、数据库长什么样

### 2.1 整体目录树

```
2025/
├── 1-27/
│   ├── TUM/                                     ← 一所大学一个文件夹
│   │   ├── __init__.py                          ← 包标记（后期批次有）
│   │   ├── TUM_00.py                            ← 主爬虫脚本
│   │   ├── TUM_01.py                            ← 辅助/诊断脚本
│   │   └── TUM_Data/                            ← 输出数据根目录
│   │       ├── Informatik_master-informatik/    ← 一个专业一个子文件夹
│   │       │   ├── Subject_Description.md       ← 专业描述
│   │       │   ├── Master_consecutive.md        ← 学位变体详情
│   │       │   ├── Bachelor_50%.md
│   │       │   ├── Modulhandbuch_BA.pdf         ← 模块手册 PDF
│   │       │   ├── Pruefungsordnung_2023.pdf    ← 考试规定 PDF
│   │       │   ├── General_Master_Application_Info.md   ← 大学通用申请信息
│   │       │   └── International_Master_Application_Guide.md
│   │       ├── Maschinenbau_master-maschinenbau/
│   │       └── ...
│   ├── Technische Universität Berlin/
│   └── ...
├── 2-2/
├── 2-9 - 年前上传原版/
├── 2-9 - 缺漏处理/
├── 2-24/
├── 3-2/
├── 3-16/
├── 3-23/
└── 3-30/
```

### 2.2 命名规范

每个子文件夹和文件名都经过 `sanitize_path` 处理：

```
原始专业名："Angewandte Informatik (M.Sc.)"
↓ 德语变音替换（ä→ae, ö→oe, ü→ue, ß→ss）
↓ 非法字符剔除（\ / * ? : " < > | → 空格）
↓ Unicode NFKD 规范化
↓ 空白压缩为 _
↓ 长度截断（文件夹≤50~60字符，文件≤35~50字符）
→ 最终名称："Angewandte_Informatik_M.Sc."
```

**命名唯一性保证**：当专业名可能与其他专业重复时，追加 URL slug 后缀：
```
Wirtschaftsinformatik_master-wirtschaftsinformatik
Wirtschaftsinformatik_master-wirtschaftsinformatik-wise
```

### 2.3 每个专业文件夹里有什么

```
专业文件夹/
├── {专业名}.md            ← 主文档，文首必含 URL: 原始链接
├── Subject_Description.md  ← 专业描述（部分早期批次）
├── {学位变体名}.md        ← 如 Bachelor 50%.md, Master, consecutive.md
├── *.pdf                   ← 原始 PDF（入学规定、模块手册等）
├── General_Master_Application_Info.md    ← 全校通用（部分批次有）
└── International_Master_Application_Guide.md
```

### 2.4 Markdown 文件内部格式

```markdown
URL: https://www.example-university.de/master/informatik

# Angewandte Informatik (M.Sc.)

## Overview
专业概述内容...

## Admission Requirements
入学要求内容...

---
## 补充申请信息 (Application Appendix)
通用申请信息...
```

---

## 三、数据怎么来的 —— 采集方法全流程

### 3.1 整体工作流

```
每周分配 8 所大学
    │
    ▼
┌─────────────────────────────────┐
│ Step 1: 页面结构分析            │
│ - 打开目标大学网站              │
│ - 判断技术类型（静态/JS/混合）  │
│ - 定位列表页 URL 和详情页结构   │
│ - 识别反爬措施（Cookie/验证码）  │
│ - 产出：结构总结  │
└──────────────┬──────────────────┘
               ▼
┌─────────────────────────────────┐
│ Step 2: 初稿脚本生成            │
│ - 由结构总结AI生成初版.py       │
│ - 遵循统一的CONFIG+CrawlerUtils  │
│   +XxxScraper 骨架              │
└──────────────┬──────────────────┘
               ▼
┌─────────────────────────────────┐
│ Step 3: 验证与修正              │
│ - 运行脚本，检查实际产出        │
│ - 对照页面源码修正选择器        │
│ - 处理边缘情况（空页/重名/外部链接）│
│ - 产出解释文件(解释.txt)记录踩坑│
└──────────────┬──────────────────┘
               ▼
┌─────────────────────────────────┐
│ Step 4: 数据归档                │
│ - 生成 Markdown 文件            │
│ - 下载 PDF（如存在）            │
│ - 回填 URL 链接（add.py）       │
│ - 可选：OCR 处理 PDF → MD       │
└─────────────────────────────────┘
```

### 3.2 页面结构分析 —— 第一件事：判断网站类型

在写任何代码之前，先打开目标大学网站，判断它属于哪种类型。这直接决定技术选型：

| 类型 | 特征 | 技术选型 |
|------|------|----------|
| **纯静态 HTML** | 列表和详情都在 HTML 源码中，不需要 JS | `requests` + `lxml` |
| **JS 筛选列表** | 列表需点击筛选按钮/下拉框才能加载 | Playwright 获取列表，requests 获取详情 |
| **JS 渲染详情** | 详情内容也依赖 JS 动态生成 | 全部 Playwright |
| **SPA/React** | 整站是单页应用，内容流式渲染 | Playwright + 等待策略 |

**判断方法**：
1. 打开浏览器 DevTools → Network 标签
2. 访问列表页，看 XHR/Fetch 请求 —— 如果有 API 返回 JSON，直接调 API 更高效
3. 右键"查看网页源代码"，对比实际渲染内容 —— 如果不一致，必用 Playwright
4. 检查是否有 `<details>`、`accordion`、`data-*` 等动态组件

### 3.3 列表页采集

列表页的目标：**获取所有硕士专业的名称和详情页 URL**。

#### 静态列表（XPath 定位）

```python
# 示例：Heidelberg University
tree = etree.HTML(res.text)
container = tree.xpath('/html/body/div[1]/main/div[1]/div[4]/div/div[2]')[0]
subject_elements = container.xpath('.//a[@aria-label and .//h4]')

tasks = []
for el in subject_elements:
    url = urljoin(BASE_URL, el.get('href'))
    name = el.get('aria-label').strip()
    tasks.append({"name": name, "url": url})
```

关键技巧：
- **优先用语义属性**（`aria-label`、`data-testid`）而非 CSS class（class 会变）
- **XPath 备选**：主 XPath 失败时尝试备选路径，不要直接崩溃
- **去重**：同一 URL 可能被多次收录，用 `dict` 或 `set` 去重

#### 动态列表（Playwright 筛选交互）

```python
# 示例：TH Ingolstadt — 需要选择下拉筛选项
with sync_playwright() as p:
    browser = p.chromium.launch(headless=False)
    page = browser.new_page()
    page.goto(START_URL, wait_until="networkidle")

    # 1. 处理 Cookie 弹窗
    accept_btn = page.locator('button[data-modal-accept-button]')
    if accept_btn.count() > 0:
        accept_btn.click()

    # 2. 选择"Master"筛选项
    page.select_option('select#filterGraduation', value="1")
    page.wait_for_selector('a.card-item', timeout=30000)  # 等结果渲染

    # 3. 提取卡片
    cards = page.locator('a.card-item').all()
    for card in cards:
        href = card.get_attribute("href")
        name = card.locator('.card-title').inner_text()
        tasks.append({"name": name, "url": urljoin(ROOT_DOMAIN, href)})
```

关键技巧：
- **等待策略**：`wait_until="networkidle"` 用于整页加载，`wait_for_selector` 用于局部元素
- **Cookie 弹窗爆破**：几乎所有德国大学都有 GDPR cookie 弹窗，用 JS `remove()` 直接物理删除
- **sleep 是必要的**：筛选后给 JS 框架 2-4 秒重新渲染时间

#### 翻页处理

```python
# 示例：Goethe University Frankfurt — 分页器
while True:
    # 处理当前页...

    next_btn = page.locator('nav[aria-label="pagination"] button[aria-label="Next"]')
    if await next_btn.is_enabled():
        await next_btn.click()
        await page.wait_for_function("""() => {
            const el = document.querySelector('nav[aria-label="pagination"] button[aria-current="page"]');
            return el && el.innerText !== 'OLD_PAGE_NUMBER';
        }""")
    else:
        break
```

### 3.4 详情页采集 —— 三种核心模式

这是整个项目最核心、最复杂、差异性最大的环节。从实践中归纳出三种模式：

#### 模式 A：固定容器提取（最简单）

适用于有明确 `<main>`、`#content`、`article` 容器的页面。

```python
# 直接取 main 节点，转 Markdown
main_node = tree.xpath('//main')[0]
raw_html = etree.tostring(main_node, encoding='unicode', method='html')
markdown = md(raw_html, heading_style="ATX")
```

**典型场景**：Heidelberg、FU Berlin、大多数 Uni 的标准化页面。

**注意**：即使有 `<main>`，也可能包含导航、侧边栏等噪音。需要：
1. 删除 `script`、`style`、`nav`、`img`、`svg`、`button` 等标签
2. 去掉 Cookie 弹窗容器（站点相关 XPath）
3. 相对链接补全为绝对 URL（`urljoin`）

#### 模式 B：线性兄弟扫描 + 哨兵熔断

适用于**无明确容器**、内容以"h1 标题 → 连续兄弟节点 → 某个特定 h2 结束"的页面。

```python
# 示例：TH Ingolstadt
h1_nodes = tree.xpath('//h1')
start_node = h1_nodes[0]
collected_html = []

curr = start_node
while curr is not None:
    # 熔断哨兵：遇到包含特定文案的 h2 立即停止
    if curr.tag == 'h2':
        text_val = "".join(curr.xpath('.//text()')).strip()
        if "Blick in die Labore" in text_val:
            print("命中熔断点，停止采集")
            break

    collected_html.append(clean_element(curr))  # 清洗当前节点
    curr = curr.getnext()  # 下一个兄弟

# 最终转 Markdown
result = to_markdown(collected_html, page_url)
```

**为什么需要哨兵熔断**：
- 德国大学页面常在专业描述后接"实验室展示""校园风光""相关新闻"等无关内容
- 如果没有停止条件，会把整个页面下半部分都扫进来
- 哨兵是**人工观察页面后设定**的固定文案，每个大学不同

**使用话术**（在修正意见中通用）：
> 使用**顶级块横扫算法（Top-level Block Scanning）**。以 `h1` 为锚点，线性向后搬运所有兄弟容器，遇哨兵 `h2` 则熔断。

#### 模式 C：关键词驱动区块（最灵活）

适用于**多个独立区块分散在页面各处**、需要按标题语义选择性提取的页面。

```python
# 示例：Universität Bayreuth — 多代架构
TARGET_KEYWORDS = [
    "Wichtigste", "essentials", "Profil", "Aufbau",
    "Career", "prospects", "前景",
    "Gut zu wissen", "Good to know",
    "Dokumente", "Downloads",
    "Studienplatz", "How do I apply"
]

# 识别折叠组件
blocks = page.locator('details, .accordion-item').all()
for b in blocks:
    title = b.evaluate("el => el.innerText.split('\\n')[0]").strip()
    if any(k.lower() in title.lower() for k in TARGET_KEYWORDS):
        # 命中关键词 → 展开 → 提取
        b.evaluate("el => el.setAttribute('open', 'true')")
        content = purified_to_md(page, b)
        result += f"## {title}\n\n{content}\n\n"
```

**典型场景**：Bayreuth（新旧双轨）、OTH Regensburg（多关键词区块）、EBS（语义回溯）。

**关键词设计原则**：
- 覆盖德/英双语（德国大学网站常混用）
- 宁可多匹配再人工筛选，不要漏掉关键信息
- 包含中文辅助词（如"一览""前景""申请"）便于多语言检索

**保底机制**（当所有关键词都未命中时）：
```python
# 按优先级尝试已知容器选择器
fallback_selectors = [
    'main', '#main', '#content', '.content', '.main-content',
    'article', '.page-content', '#primary'
]

for selector in fallback_selectors:
    node = page.locator(selector).first
    if await node.count() > 0 and await node.is_visible():
        found_node = node
        break

if not found_node:
    # 最终手段：用 body，但在 JS 层移除导航和页脚
    found_node = page.locator('body')
    found_node.evaluate("""(body) => {
        const junk = body.querySelectorAll('nav, footer, header, script, style');
        junk.forEach(el => el.remove());
    }""")
```

### 3.5 PDF 与附件下载

```python
def download_pdf(url, save_dir, label):
    filename = sanitize(label)
    if not filename.lower().endswith('.pdf'):
        filename += ".pdf"
    path = os.path.join(save_dir, filename)

    # 已存在则跳过（断点续爬核心）
    if os.path.exists(path):
        print(f"PDF 已存在: {filename}")
        return

    res = session.get(url, stream=True, timeout=20)
    res.raise_for_status()
    with open(path, 'wb') as f:
        for chunk in res.iter_content(8192):
            f.write(chunk)
```

**PDF 发现策略**（按优先级）：
1. 遍历 `//a`，检查 `href` 是否以 `.pdf` 结尾
2. 检查链接文本是否含关键词：`Modulhandbuch` / `Module Handbook` / `Prüfungsordnung` / `Zugangsordnung`
3. 正则匹配防软连字符：`re.search(r'modul­handbuch', text, re.I)` — **比 `if kw in text.lower()` 更可靠**

**PDF 大小控制**（实战经验）：

来自 OTH Regensburg 的教训：
> "很多大学模块手册中入学内容相当有限，但以防万一，下了。只下载不大于 500KB 的。"

不同大学按需设置阈值，避免下载几百 MB 的无用附件。

### 3.6 补充信息处理

#### 申请要求单独页面

许多大学将"录取要求"放在独立页面（而非专业详情页内）。需要：

1. 在详情页中查找侧边栏链接（含关键词 `zugang` / `bewerbung` / `admission` / `application`）
2. 跳转到申请页，提取内容
3. 追加到专业 Markdown 末尾，以 `---\n## 补充申请信息` 分隔

```python
# 查找侧边栏中的申请要求链接
apply_link = page.locator('a:has-text("Infos zur Bewerbung"), a:has-text("Bewerbung")').first
if apply_link.count() > 0:
    sub_url = urljoin(ROOT_DOMAIN, apply_link.get_attribute("href"))
    sub_page = context.new_page()
    sub_page.goto(sub_url, wait_until="domcontentloaded")
    appendix = extract_content(sub_page)
    main_md += "\n\n---\n## 补充申请信息\n\n" + appendix
```

#### 通用申请信息分发（add.py）

当大学有一个全校统一的 Master 申请说明页面时，使用 `add.py` 工具：

1. 用独立脚本抓取通用申请页面（处理折叠面板等交互）
2. 保存为 `General_Master_Application_Info.md`
3. 批量复制到**每个专业文件夹**中

此外，`add.py` 还负责**链接回填**——检查每个 `.md` 文件开头是否有 `**Original Program Link:**`，没有则补上。

---

## 四、技术栈与演进路线

### 4.1 核心依赖

```
requests          HTTP 请求（静态页面）
lxml              HTML/XPath 解析
beautifulsoup4    HTML 解析（早期使用）
markdownify       HTML → Markdown 转换
playwright        无头浏览器（JS 渲染页面）
selenium          无头浏览器（早期使用，已逐步被 Playwright 替代）
```

### 4.2 五个阶段的演进

**阶段 1：早期探索（1-27）**

- 技术：`requests` + `BeautifulSoup` + `Selenium`
- 特点：还在摸索德国大学网站特征
- `basic_spider.py`——尝试建立通用爬虫框架（后来发现每个大学都太特殊，不如独立脚本）
- RWTH Aachen 的任务是批量下载考试规定 PDF（Amtliche Bekanntmachungen），产出数百个 MD+PDF 文件对
- TUM 需要 Selenium 处理 JS 翻页和筛选

**阶段 2：静态为主（2-2）**

- 技术：`requests` + `lxml` + `markdownify`
- 特点：这批大学恰好多为静态页面，`requests` 直接够用
- 建立了 `sanitize`、`safe_request`（带延迟和重试）、`html_to_md` 等函数
- `02.py` 辅助脚本模式出现——当主脚本不够用时，写第二个专门处理特殊情况

**阶段 3：JS 攻坚（2-9）**

- 技术：Playwright（`sync_playwright` + `async_playwright`）
- 关键突破：攻克了项目中最难的三个网站
  - **Goethe Frankfurt**：流式渲染 + 不可视时卸载，两套折叠模板，花了一整天
  - **Uni Köln**：React + 操作密度检测，每获取 2 个专业就要切换 IP
  - **Uni Würzburg**：页面格式极度混乱，无通用格式，部分专业是外部链接
- `add.py` 工具诞生，链接回填 + 通用信息分发
- 出现了"缺漏处理"文件夹——对同一批次的第二轮补量

**阶段 4：成熟期（2-24）**

- 技术：Playwright 多代架构
- Bayreuth 脚本是最成熟的一个：
  - OnePager 穿透（发现跳转页面 → 自动跟随）
  - 英语授课中转框检测
  - 现代新版 / 旧版双轨判定（`details` 存在 → 新版折叠提取，否则 → 旧版 main 提取）
  - JS 层直接清理 DOM 噪音（比 lxml 后端清洗更彻底）
  - `nuke_cookie`：用 `evaluate` 直接删除所有可能的 cookie 弹窗元素

**阶段 5：批量化（3-2 至 3-30）**

- 技术：Playwright + Requests 混合模式，sync_playwright 统一
- 每个大学文件夹都有 `__init__.py`，向模块化过渡
- 出现了"注释"子文件夹——每个脚本有一份带中文注释的副本
- `解释.txt` 文件标准化——每个有特殊情况的大学都记录了踩坑说明
- 3-30 批次出现 OCR 实验（`PaddleOCR-VL` 将 PDF 再转回 MD 以便全文检索）

### 4.3 技术选型决策树

```
开始分析一所新大学
│
├─ 列表页在 HTML 源码中可见？
│   ├─ 是 → requests + lxml 获取列表
│   └─ 否 → Playwright 获取列表（筛选/翻页/懒加载）
│
├─ 详情页在 HTML 源码中可见？
│   ├─ 是 → requests + lxml 获取详情
│   └─ 否 → Playwright 获取详情
│
├─ 是否有 Cookie/Consent 弹窗？
│   └─ Playwright → evaluate 物理删除
│
└─ 是否需要 VPN/特殊网络？
    └─ 在 解释.txt 中记录
```

---

## 五、脚本架构约定

### 5.1 三段式结构（每个脚本的标准骨架）

```python
# ============ 第一段：CONFIG 全局配置 ============
CONFIG = {
    "UNIVERSITY_NAME": "XXX",
    "ROOT_DOMAIN": "https://www.xxx.de",
    "START_URL": "https://www.xxx.de/...",
    "OUTPUT_DIR": "XXX_Data",
    "USER_AGENT": "Mozilla/5.0 ...",
    # 业务相关配置
    "SENTINEL_END_TEXT": "...",    # 哨兵文案（模式B专用）
    "SEMANTIC_HEADLINES": [...],   # 关键词列表（模式C专用）
    "PDF_KEYWORDS": [...],         # PDF 匹配关键词
}

# ============ 第二段：CrawlerUtils 工具层 ============
class CrawlerUtils:
    @staticmethod
    def sanitize_path(name, is_folder=True): ...
        # 德语变音 → ASCII → 去非法字符 → 截断

    @staticmethod
    def clean_element(element): ...
        # 删除 script/style/nav/img 等噪音标签

    @staticmethod
    def to_markdown(html_content): ...
        # HTML → markdownify → 排版修复

# ============ 第三段：核心爬虫类 ============
class XxxScraper:
    def __init__(self): ...
    def run(self): ...
        # 主流程：fetch_list → for each → process_detail
    def fetch_list(self): ...
        # 列表页 → [{name, url, meta}]
    def process_detail(self, task): ...
        # 详情页 → sanitize → 清洗 → 提取模式ABC → 写MD → 下PDF
    def download_pdf(self, url, label, dir): ...
        # 流式下载 + 去重
```

**设计原则**：
- **每个大学一个独立 `.py` 文件**，不共享脚本（因为差异太大）
- **CrawlerUtils 内联**，不提取为公共库（每个脚本可能微调）
- **主爬虫类以大学缩写命名**：`THIScraper`、`BayreuthScraper`、`EBSScraper`

### 5.2 辅助脚本类型

| 脚本类型 | 命名 | 用途 |
|----------|------|------|
| 主爬虫 | `{校名}.py` / `{缩写}.py` | 完整采集流程 |
| 辅助爬虫 | `02.py` / `03.py` | 处理特殊情况（不同页面类型、补充采集） |
| 通用信息 | `Application and Enrollment.py` | 抓取大学通用申请页面 |
| 链接回填 | `add.py` | 在已有 MD 文件顶部补 `Original Program Link` |
| 诊断脚本 | `02.py`（Bayreuth） | 打印 DOM 中所有链接，辅助 XPath 编写 |
| 列表工具 | `tool.py`（Köln） | 从预收集的 URL 列表文件读取并采集 |

### 5.3 解释文件约定

每个有特殊情况的大学目录下放置 `解释.txt`，记录：

- 技术难点（如流式渲染、密度检测、Cloudflare）
- 页面结构描述（几种类型、有无外部链接）
- 特殊处理方案（为什么用了 2 个脚本）
- 注意事项（VPN 需求、PDF 大小限制）

这是项目的**隐性知识库**，对新开发者理解"为什么这么写"至关重要。

### 5.4 注释文件夹

部分批次（3-16、3-23）有 `{批次号} - 注释` 子文件夹，包含每个脚本的注释版副本。这些用于：
- 生成 AI 理解和维护代码
- 标注每个选择器的作用
- 记录提取逻辑的判断依据

---

## 六、需要注意的点（实战踩坑记录）

### 6.1 反爬对抗

**Cookie / Consent 弹窗**（几乎 100% 的德国大学都有）：

不要试图"正常点击接受"——直接用 JS 物理删除弹窗 DOM：

```python
page.evaluate("""() => {
    ['#usercentrics-root', '.cookie-notice', '.banner-wrapper',
     'div[class*="cookie"]', 'div[id*="consent"]'].forEach(s => {
        const el = document.querySelector(s);
        if(el) el.remove();
    });
    document.body.style.overflow = 'auto';
}""")
```

**Cloudflare 验证**（FH Aachen）：

网站会在访问时触发 Cloudflare 浏览器验证。对策：
- 使用 Playwright（带完整浏览器指纹）而非 requests
- 关闭 `headless` 模式，让验证通过后再切回

**操作密度检测**（Uni Köln）：

网站使用 React，检测鼠标移动和操作频率。每抓取 2 个专业就被封。对策：
- 每次只开一个专业的页面
- 每次操作间隔足够长（随机 2-4 秒）
- 手动切换代理节点（尝试过 IP 池但效果不好）
- **刚需 VPN / 梯子**

**浏览器验证链路**（EBS）：

Playwright 访问时偶尔出现长时间加载。对策：
- `wait_until="domcontentloaded"` 而非 `networkidle`（后者可能无限等待）
- 超时设置到 60 秒
- 如果经常失败，考虑使用 Playwright 调试接口（`page.pause()` / CDP）

### 6.2 网站结构复杂度

**同一大学多种页面类型**：

Hochschule Karlsruhe 有 2 种不同的详情页结构，部分专业的入学规定在独立跳转页面。需要：
- 在 `process_detail` 开头判断页面类型（通过 XPath 探测特征元素）
- 不同类型走不同提取分支

**流式渲染 + 不可视时卸载**（Goethe Frankfurt）：

页面内容只有在浏览器视口内时才渲染，滚动出视口即卸载 DOM。导致：
- 右键"查看源代码"完全看不出有效内容
- `requests.get()` 拿到的是空壳
- 必须用 Playwright 全量渲染后 `.innerHTML()` 直接取

**cHash 匹配跳转**（Tübingen）：

页面使用 Typo3 的 cHash 参数做缓存校验，URL 必须带有正确的 cHash 值才能访问。对策：
- 用无头浏览器打开列表页，从渲染出的链接中取完整 URL（自带 cHash）
- 不要自己拼接 URL

**外部链接与格式混乱**（Würzburg）：

部分硕士专业指向完全不同的外部网站（如 `ilf-frankfurt.de`），格式各异。对策：
- 保底机制：识别 `<main>` / `#content` → 全量抓取
- 对抓取结果做长度检查（> 100 字符才算有效）
- 外部网站的超时时间设置得更长

### 6.3 编码问题

德国大学网站编码不统一：
- 列表页常为 UTF-8
- 详情页可能是 Latin-1、ISO-8859-1 等本地编码

**正确做法**：
```python
res = session.get(url, timeout=30)
# 不要硬编码 utf-8，让 requests 自行探测
res.encoding = res.apparent_encoding
```

同时对两种编码都做兜底：
```python
try:
    text = res.text
except:
    res.encoding = 'utf-8'
    text = res.text
```

### 6.4 PDF 相关注意事项

1. **大小审计**：先检查 `Content-Length` 头，大于阈值（如 500KB）的跳过或询问
2. **已存在跳过**：写入前检查 `os.path.exists(path)`，支持断点续爬
3. **文件名安全**：PDF URL 的最后一节可能含非法字符，需要 sanitize
4. **流式下载**：用 `stream=True` + `iter_content`，避免大文件撑爆内存
5. **Content-Disposition**：部分服务器通过此头返回真实文件名，应优先使用

### 6.5 新旧网站交替

THGA（Technische Hochschule Georg Agricola）的教训：
> "这个大学的文件可能是此时时间问题，刚好处于新旧交替"

对策：
- 优先适配新结构，同时保留旧结构回退路径
- 在解释文件中标记"之后维护需检查 PDF 是否改名"
- 定期（如每季度）重新运行验证脚本，检查是否有链接失效

---

## 七、重点总结

### 7.1 脚本复用而非重写

CrawlerUtils 模式的本质：**80% 的代码不变，20% 的选择器按站替换**。

拿到一所新大学时，需要改的几乎只有：
- `CONFIG` 字典（校名、URL）
- `fetch_list` 中的选择器
- `process_detail` 中的提取模式（选 A/B/C 之一）

`sanitize_path`、`to_markdown`、`clean_element`、`download_pdf` 这些是稳定层。

### 7.2 断点续爬是命脉

```
if os.path.exists(major_dir):
    print(f"续爬 - 跳过已存在文件夹：{safe_major_name}")
    return
```

这个逻辑保证了：
- 脚本中断后可以无痛重跑，不会重复下载
- 支持分批补量（第一批抓 80%，第二批针对缺失专业单独补）
- 脚本调试时可以反复运行而不产生冗余数据

### 7.3 三种提取模式的选择判断

| 页面特征 | 对应模式 | 关键参数 |
|----------|----------|----------|
| 有明确的 `<main>` / `#content` / `article` 容器 | 模式 A：固定容器 | 容器 XPath |
| 内容从 h1 开始线性排列，后有无关内容 | 模式 B：线性扫描+哨兵 | 哨兵文本 |
| 内容分散在多个折叠面板/区块中 | 模式 C：关键词驱动 | 关键词列表 |

三种模式可以**组合使用**——如 Bayreuth 先判断新版/旧版，新版用模式 C 匹配折叠项，旧版用模式 B 取 main 之后的内容。

### 7.4 防御性编程

- **单页失败不中断**：`try/except` 包裹 `process_detail`，`continue` 下一条
- **超时设置要长**：外部网站可能很慢，`timeout=60` 是合理默认值
- **随机延迟**：`time.sleep(random.uniform(1.0, 3.0))`，避免触发频率限制
- **请求重试**：`Retry(total=5, backoff_factor=3)` + `HTTPAdapter`
- **内容长度检查**：抓取结果 < 100 字符时打日志告警，可能页面加载失败

### 7.5 从静态到动态的技术选型判断

```
打开列表页 → 右键查看源代码 → 搜索专业名
   │
   ├─ 找到了 → requests 足够
   │
   └─ 找不到 → 按 F12 → Network → 找 XHR/API
         │
         ├─ 有 JSON API → 直接调 API（最优）
         │
         └─ 无 API → Playwright
              │
              ├─ 仅列表需要交互 → 列表 Playwright + 详情 requests
              └─ 详情也需渲染 → 全部 Playwright
```

### 7.6 数据的可溯源性与可维护性

- 每个 MD 文件文首写 `URL: {原始链接}` 或 `**Original Program Link:** {url}`
- 每个特殊处理在 `解释.txt` 中留下记录
- 脚本的"注释"版本供后续 AI 理解和修改
- 目录命名保留 URL slug 确保唯一性

---
